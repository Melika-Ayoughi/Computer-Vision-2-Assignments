function [filtered_frame, filtered_descriptors] = filter_feature_points(frame, descriptors)	 % DOCSTRING_GENERATED
 % FILTER_FEATURE_POINTS		 [Should filter out points that belong to the background]
 % INPUTS 
 %			frame = ..
 %			descriptors = ..
 % OUTPUTS 
 %			filtered_frame = ..
 %			filtered_descriptors = ..



filtered_frame = frame; % TODO: background filtering. how though?
filtered_descriptors = descriptors;

end